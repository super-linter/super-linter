{
  "system_prompt": "Use the search tool when needed. Tool call limit: 3.",
  "tools": [
    {
      "name": "search",
      "description": "Search the documentation for a specific query.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "The documentation search query."
          }
        },
        "required": ["query"]
      }
    }
  ]
}
