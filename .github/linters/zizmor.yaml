---
rules:
  # TODO: disable these ignores after we pin versions
  unpinned-uses:
    ignore:
      - ci.yml
      - cd.yml
      - dependabot-automation.yaml
      - lint-commit.yaml
      - stale.yml
      - thank_contributors.yaml
